import { INotebookTracker, NotebookPanel } from '@jupyterlab/notebook';

export function initBubbles(notebookTracker: INotebookTracker): void {
  // listen for new cells added
  //check if they have metadata.bubbles and if so create bubbles for them
}
